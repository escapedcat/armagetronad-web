== /private/tmp/claude-502/-Users-user-data-src-armagetronad/8b714b4b-bf5d-42ff-83a1-cb627e3f3086/scratchpad/live-mp-firefox/console.log
   engine firefox 154.0.1 (headless=true)
   url    https://escapedcat.github.io/armagetronad-web/armagetronad.html
   477 lines, 428 of them not harness echoes

PASS  X1   the transcript is a run against the public https deployment (driver: https://escapedcat.github.io/armagetronad-web/armagetronad.html; page: "WebSocket counter installed; page origin is https:")
PASS  X2   the module made 96 ws:// connection attempts, inside the 96-100 band (4 masters x 24-25 attempts each) -- reproduced against the real deployment, which is what completes task 3
PASS  X3   two independent counts agree: 96 from the in-page WebSocket wrapper, 96 browser lines saying "...establish a connection to the server at ws://..." from the browser
       and the totals bracket the query: 0 attempts BEFORE the master query was started, 96 after the menu was dismissed twice
PASS  X4   all attempts went to exactly 4 distinct ws:// master endpoints
        24  ws://master1.armagetronad.org:4533/
        24  ws://master2.armagetronad.org:4533/
        24  ws://master3.armagetronad.net:4533/
        24  ws://master4.armagetronad.net:4533/
PASS  X5   first attempt to last spans 20.04s (band 18-22s) -- 4 masters x sn_Connect's 5s timeout, not the 26s the plan states
PASS  X6   nothing was attempted before the master query (0) and nothing more was attempted after it (96 total vs 96 at the end of the query) -- the ~100 lines are bounded and stop when the fourth master times out
PASS  X7   after both dismissals the canvas still has a live GL context with no error ("alive, gl err=0x0")
PASS  X8   no forbidden text in the run, and all 192 [EXCEPTION] lines are one of the TWO permitted mixed-content texts on a ws:// endpoint
       permitted, verbatim:
         "...establish a connection to the server at ws://..."
         "...was interrupted while the page was loading...."
       The rule is NOT loosened: a third text, or either text on a line that
       does not name a ws:// endpoint, still fails.
PASS  X9   Firefox produced 0 lines matching /mixed content/i while 96 attempts were stopped -- so a gate that grepped for "Mixed Content" would conclude Firefox is NOT blocking. It is: task 3's scheme differential is 27 attempts over http: against 97 over https:, same build, same script
note  this is a POSITIVE assertion that the message is absent, not an oversight.
note    Firefox's text for a blocked mixed-content WebSocket is the same sentence it
note    uses for an ordinary connection failure, so the phrase never appears.
PASS  X10  no "until TIMED OUT" anywhere in the file (0 hits) -- the driver records this and keeps going, so it is the only sign of a wait that never came
PASS  X11  every 404 is /favicon.ico (2 total, 0 other) -- the browser asks for it once per navigation and the deployment has none

PASS  XZ   every declared check ran: 11 declared, 11 executed

ALL CHECKS PASSED
